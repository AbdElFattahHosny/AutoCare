@model CarEditViewModel
@inject SignInManager<ApplicationUser> SignInManager
@{
    ViewData["Title"] = "Edit";
    Layout = "";
}


<div class="row">
    <div class="col-md-4">
        <form method="post" asp-action="Edit" asp-route-id="@ViewBag.id" onsubmit="return jQueryAjaxPost(this);">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>      
            <div class="form-group">
                <label asp-for="CarNumbers" class="control-label"></label>
                <input asp-for="CarNumbers" class="form-control" style="width:200%" />
                <span asp-validation-for="CarNumbers" class="text-danger"></span>
            </div>
            @*<div class="row">
        <div class="col-md-6">*@
            <div class="form-group">
                <label asp-for="Type" class="control-label"></label>
                <input asp-for="Type" class="form-control" style="width:200%" />
                <span asp-validation-for="Type" class="text-danger"></span>
            </div>
            @*</div>
        </div>*@
            @*</div>*@
            @*</div>*@
            @*<div class="row">
        <div class="col-md-6">*@
            <div class="form-group">
                <label asp-for="Model" class="control-label"></label>
                <input asp-for="Model" class="form-control" style="width:200%" />
                <span asp-validation-for="Model" class="text-danger"></span>
            </div>
            @*<div class="row">
        <div class="col-md-6">*@
              @if ((SignInManager.IsSignedIn(User) && !User.IsInRole("User")))
            {
                <div class="form-group">
                    <label asp-for="ownerId" class="control-label"></label>
                <select asp-for="ownerId" class="form-control" asp-items="@(new SelectList(Model.owners,"Id","NationalNumber"))" style="width:200%">
                    <option selected disabled>--Choose Owner please--</option>
                </select>
                <span asp-validation-for="ownerId" class="text-danger"></span>
            </div>
            }
            @*</div>
                </div>
            </div>
        </div>*@
            <div class="form-group">
                <div class="col-md-2 offset-md-3">
                    <input type="submit" value="Submit" class="btn btn-primary btn-block" style="width:100px" />
                </div>
            </div>
        </form>
    </div>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
